# Worker secrets are set via `wrangler secret put AUTH_SECRET` in deployed
# environments. For local development, copy `apps/edge/.dev.vars.example` to
# `apps/edge/.dev.vars` and set values there instead — Wrangler loads that
# file automatically for `wrangler dev`.
AUTH_SECRET=change-me

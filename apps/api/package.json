{
  "name": "@tunnel/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "migrate:remote": "wrangler d1 migrations apply DOMAINS --remote",
    "deploy": "vp run migrate:remote && wrangler deploy",
    "lint": "vp lint",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vp test run --config vitest.config.ts --passWithNoTests"
  },
  "dependencies": {
    "@tunnel/config": "workspace:*",
    "@tunnel/core": "workspace:*",
    "@tunnel/protocol": "workspace:*",
    "effect": "catalog:",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.2",
    "@cloudflare/workers-types": "^5",
    "@tunnel/tsconfig": "workspace:*",
    "typescript": "^5",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4"
  }
}
